<?php
/**
 * CSDL MySQL
 *
 * Thuật ngữ Database cơ sở dữ liệu
 * - Database có nhiều table(bảng), chứa các dữ liệu cùng một đối tượng
 * - Tên bảng viết số nhiều và viết thường
 * - 1 Table(bảng) chứa nhiều Record(bản ghi)
 * - Column
 * - Khóa chính: phân biệt bản ghi, không được trùng nhau
 * - Khóa ngoại: Liên kết các bảng lại với nhau
 * - Mội số mối quan hệ
 * - 1 -1 , 1-n, n-n
 *#################
 *
 * Insert Into()
 * Values()
 *
 * SELECT * FROM WHERE...
 * LIMIT lấy giới hạn bản ghi tính từ vị trí đầu tiên
 * LIMIT 5,10 Lấy bản ghi tính từ vị trí 5 đến 10
 *
 *
 *
 * UPDATE products
 * SET name = '...'
 * WHERE id = ....
 *
 *
 * DELETE FROM ...
 */